import styled from 'styled-components';

const CorrectAnswerOptions = styled.div`
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin: 10px 0;

  & .option {
    width: 50%;
    text-align: left;
  }

  & input {
    
  }
`;

export default CorrectAnswerOptions;
